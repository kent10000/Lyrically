<UserControl x:Class="LyricallyGUI.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="1" BorderThickness="0,2,0,2" BorderBrush="Black" IsReadOnly="True"  FontFamily="Micross" x:Name="Main"/>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox BorderThickness="0" Grid.Column="0" IsReadOnly="True" VerticalAlignment="Center" FontFamily="Micross" FontSize="20" Text="Settings" x:Name="Title"/>
            <Button x:Name="WSettings" Click="ButtonSettings_OnClick" FontSize="15" Padding="5" Grid.Column="1" Background="Transparent" BorderThickness="0">&#x2699;</Button>
        </Grid>
        
        
    </Grid>
</UserControl>
